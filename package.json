{
  "name": "comp5411-rendering-project",
  "version": "1.0.0",
  "description": "## How to run First, ```npm install```",
  "main": "server.js",
  "dependencies": {
    "express": "^4.16.4"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  }
}
